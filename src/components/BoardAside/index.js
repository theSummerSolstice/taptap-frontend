import React from 'react';
import styles from './BoardAside.module.scss';

const BoardAside = ({ children }) => {
  return (
    <div className={styles.container}>
      board aside
    </div>
  );
};

export default BoardAside;
